/*(function ($) {
	'use strict';

	$.fn.dialog = function (option) {
		var opts = $.extend({}, $.fn.dialog.defaults, options);
		
		$(opts.html).appendTo(opts.appendTo);
	};

	$.fn.lightbox.defaults = {
        appendTo: 'body',
        html: '<input />',
    };

}(jQuery));*/
